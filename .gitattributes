# Git LFS tracking for xcframework binaries
Frameworks/** filter=lfs diff=lfs merge=lfs -text

# Git LFS tracking for snapshot test baselines
**/__Snapshots__/**/*.png filter=lfs diff=lfs merge=lfs -text
